const DL_SELECTOR = "[js-draggable_list]";

window.addEventListener('load', function () {
  const draggableList = document.querySelector(DL_SELECTOR);
  console.log('draggable list is here', draggableList)
})
