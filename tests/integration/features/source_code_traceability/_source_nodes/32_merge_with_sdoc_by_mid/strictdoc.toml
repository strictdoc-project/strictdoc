[project]

features = [
  "REQUIREMENT_TO_SOURCE_TRACEABILITY",
]

source_nodes = [
  { "src/" = { uid = "SRC-NODES-BASE", node_type = "REQUIREMENT" } }
]

exclude_source_paths = [
    "test.itest"
]
